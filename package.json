{
    "private": true,
    "scripts": {
        "build": "node node_modules/prisma/build/index.js generate --schema=backend/prisma/schema.prisma && cd frontend && npm install && npx vite build"
    },
    "engines": {
        "node": "22.x"
    },
    "devDependencies": {
        "@vitejs/plugin-react": "^4.2.1",
        "autoprefixer": "^10.4.19",
        "postcss": "^8.4.38",
        "prisma": "^5.22.0",
        "tailwindcss": "^3.4.3",
        "vite": "^5.2.0"
    },
    "dependencies": {
        "@prisma/client": "^5.22.0"
    }
}