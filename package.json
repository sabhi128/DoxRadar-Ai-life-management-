{
    "private": true,
    "scripts": {
        "postinstall": "cd backend && npm install && npx prisma generate",
        "build": "cd frontend && npm install && npm run build"
    },
    "engines": {
        "node": "22.x"
    }
}